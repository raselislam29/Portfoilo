import { EDUCATION } from '../constants';
export default function Education() {
  return <section id="education" className="section wrap"><div className="section-top"><div><p className="eyebrow">04 / FOUNDATIONS</p><h2>Built on a strong foundation.</h2></div></div><div className="education-grid">{EDUCATION.map((education, index) => <article className="education-card" key={education.institution}><div className="education-top"><span className="degree-icon" aria-hidden="true">{index === 0 ? 'BS' : 'AAS'}</span><span>{education.period}</span></div><h3>{education.institution}</h3><p>{education.degree}</p><div className="education-bottom"><span className="gpa">{education.gpa.split(' ')[0]} <small>GPA</small></span><span>Magna Cum Laude</span></div>{index === 0 && <p className="honors">{education.recognition.split(' · ')[1]}</p>}</article>)}</div></section>;
}
