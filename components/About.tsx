import { PERSONAL_INFO, SKILLS } from '../constants';
export default function About() {
  return <section id="about" className="section wrap about-grid"><div><p className="eyebrow">03 / THE WAY I BUILD</p><h2>A builder’s mindset.<br /><em>A security foundation.</em></h2><p className="about-copy">{PERSONAL_INFO.bio}</p><p className="about-copy">From leading an Agile team on OpenMD to building backend systems in healthcare, I care about the whole picture: useful experiences, dependable data, and thoughtful access controls.</p><div className="about-meta"><span>↗ {PERSONAL_INFO.location}</span><span>English & Bengali</span></div></div><div className="skills-panel">{([['primary', '01', 'Backend & automation'], ['secondary', '02', 'Full stack & cloud'], ['security', '03', 'Security by design']] as const).map(([category, number, title]) => <div className="skill-group" key={category}><h3><span>{number}</span>{title}</h3><div className="tags">{SKILLS.filter(s => s.category === category).map(s => <span key={s.name}>{s.name}</span>)}</div></div>)}</div></section>;
}
